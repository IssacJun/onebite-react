switch (key) {
    case "dog":
        break;
        
    case "cat":
        break;

    default:
        break;
}
// 위에서 아래로 일치여부 확인하고, case에서 일치했을 때
// 그때부터 그 아래로 쭉 실행함
// 그래서 break을 추가해서 이를 멈추게 함

// case에 해당하지 않으면 default를 실행하고
// default가 없으면 아무것도 실행되지 않음